# Die Lichtmaschine - Python Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
PyQt6>=6.5.0
PyYAML>=6.0
numpy>=1.24.0
pandas>=2.0.0
pillow>=10.0.0
requests>=2.28.0

# Audio features
sounddevice>=0.4.6
soundfile>=0.12.0
librosa>=0.10.0
scipy>=1.10.0
pygame>=2.5.0

# DMX USB device detection
pyserial>=3.5

# GDTF fixture definitions (DIN SPEC 15800), see docs/gdtf-integration-plan.md
pygdtf>=1.4.5
# GLB mesh loading for GDTF 3D models (visualizer)
trimesh>=4.0.0

# 3D Graphics (if needed)
moderngl>=5.8.0
pyglm>=2.7.0

# Offline media rendering (headless visualizer -> MP4). GIF/PNG output only
# needs Pillow (above); this is the FFmpeg backend for the optional MP4 export.
imageio-ffmpeg>=0.4.9

# Development/Testing
pytest>=7.0.0

# Note: On Linux, you may need system packages for audio:
# Ubuntu/Debian: sudo apt-get install libportaudio2
# Fedora: sudo dnf install portaudio