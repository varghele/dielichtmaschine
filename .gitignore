# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.manifest
*.spec
!qlcshowcreator.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Virtual environments
venv/
ENV/
env/
.venv

# IDE
.idea/
.vscode/
.claude/
*.swp
*.swo
*~
.DS_Store

# Project specific
binary/
effects/effects.json
release/
nul
scratch/

# Generated demo media: the small stills (.png) and GIFs (.gif) are committed
# for the README, but never the large MP4 renders — regenerate any of it with
# `python -m demos.generate_media <rig>`.
demos/media/**/*.mp4

# Scratch / versioned project configs at the project root.
# `config.yaml` (the active project) is intentionally tracked;
# stash old configs / audio / renders under `archive/` (gitignored below).
/conf_*.yaml

# Local-only graveyard for old configs, scratch audio, anything you want
# kept on disk but not in version control. Renamed from `showcreator_archive/`
# in v1.0 release prep; the previously-tracked configs (conf_v2..v9 etc.)
# moved here and were untracked at the same time.
/archive/

# Stray default-name show file Windows creates.
archive/shows/New Show.csv

# Local scratch shows + working-tree litter (user triages manually).
archive/shows/NTEST.csv
archive/shows/renders/
archive/shows/audiofiles/voll_mit_vox_*
archive/shows/audiofiles/backingtrack_*
/tester.yaml
/workspace_generated.qxw
/workspace_saved.qxw

# GDTF companion .qxf output from the .qxw exporter (generated per export).
/gdtf_companion_fixtures/
